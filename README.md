# Final Hackathon

This workspace contains a Vite + React client (`/client`) and an Express server (`/server`).

Run both in development:

```bash
npm run dev
```

This runs both the client and server concurrently.
